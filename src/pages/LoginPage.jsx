import React from 'react';
import AuthScreen from '../features/auth/AuthScreen';

const LoginPage = () => {
    // This component is simple because AuthScreen handles all its own logic
    return <AuthScreen />;
};

export default LoginPage;